# Future platform blocks

Role picker and B2B/patient shells stay in the repo so they can be wired back later.

**Product entry:** `/` → `/login` → `/doctor`.

| Path | What |
|---|---|
| `/future/roles` | Five-role switcher (1.1, 2.1, 3.1/3.4, 4.1, 5.1) |
| `/employer` `/insurance` `/government` `/patient` | Existing dashboards, not in clinician nav |

Do not link this from clinician login. Open `/future/roles` only when reconnecting the full platform demo.
